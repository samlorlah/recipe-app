<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Recipe App</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Home", root_path, class:"nav-link active" %>
          </li>
          <li class="nav-item">
            <%= link_to "Foods", user_foods_path(current_user.id), class:"nav-link active" %>
          </li>
          <li class="nav-item">
            <%= link_to "Recipes", user_recipes_path(current_user.id), class:"nav-link active" %>
          </li>
          <li class="nav-item">
            <%= link_to "Public Recipes", user_public_recipes_path(user_id: current_user.id, public: true), class:"nav-link active" %>
          </li>
        <% end %>
      </ul>
        <div class="gap-3">
          <% if user_signed_in? %>
          <%= link_to "Sign Out", destroy_user_session_path, class:"btn btn-outline-danger",'data-turbo-method': :delete%>
          <% else %>
          <%= link_to "Sign Up", new_user_registration_path, class:"btn btn-outline-info" %>
          <%= link_to "Sign In", new_user_session_path, class:"btn btn-outline-primary" %>
          <% end %>
        </div>
    </div>
  </div>
</nav>